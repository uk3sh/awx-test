# inventories/digitalocean_inventory.yml
plugin: community.digitalocean.digitalocean
api_token: "{{ lookup('env', 'DO_API_TOKEN') }}"
regions:
  - nyc1
  - nyc2
  - nyc3
validate_certs: true
group_by:
  - region
  - tags
compose:
  ansible_host: public_ipv4
